{
  "name": "figma-elementor-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin that extracts designs into an Elementor-ready ZIP package.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
